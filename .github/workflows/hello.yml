name: devops
on:
  workflow_dispatch:
    

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: copy code
        run: echo "this is code copy step"
  build:
    needs: code
    runs-on: ubuntu-latest
    steps:
      - name: build the code
        run : echo "this is building the code stage"
